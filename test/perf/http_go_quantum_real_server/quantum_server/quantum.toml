[package]
name = "quantum_http_real_server_benchmark"
version = "0.1.0"
edition = "2026"

[source]
root = "src"
include = ["**/*.qn"]
exclude = ["build/**"]

[dependencies]
http = { path = "../../../../packages/http", version = "0.1.0" }
