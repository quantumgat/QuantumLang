# BSON Negative Fixtures

Required malformed inputs:

```text
short document length
declared length mismatch
missing document terminator
unknown type tag
cstring field name with embedded null
invalid string length
invalid ObjectId length
unexpected EOF
```
