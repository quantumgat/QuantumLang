// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct ChannelState {
  channel: ChannelId
  bufferedItems: usize
  closed: bool
}
pub extern "qnpkg" {
  pub fn channelState(channel: ChannelId, bufferedItems: usize, closed: bool) -> ChannelState
}
