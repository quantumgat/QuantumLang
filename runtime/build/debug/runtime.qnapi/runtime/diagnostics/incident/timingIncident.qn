// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub enum TimingIncidentKind {
  SecretBranch,
  SecretMemoryAccess,
  EarlyExitEquality,
  TimingLeak,
}
pub struct TimingIncident {
  kind: TimingIncidentKind
  recoverable: bool
}
