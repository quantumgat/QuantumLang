// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RedactionPolicy {
  redactSecrets: bool
  redactPaths: bool
  productionSafe: bool
}
pub extern "qnpkg" {
  pub fn redactionPolicy() -> RedactionPolicy
}
