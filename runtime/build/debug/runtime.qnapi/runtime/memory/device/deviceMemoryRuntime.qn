// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeDeviceMemoryContract {
  target: string
  addressSpace: string
  hostTransferRequired: bool
  pinnedBufferSupported: bool
}
