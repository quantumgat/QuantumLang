// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimePinnedIoBuffer {
  address: usize
  bytes: usize
  aligned: bool
  pinned: bool
  pageLocked: bool
}
