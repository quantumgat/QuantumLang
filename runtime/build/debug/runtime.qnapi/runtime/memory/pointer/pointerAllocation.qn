// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimePointerAllocation {
  allocationId: u64
  generation: u64
  domainId: u64
  bytes: usize
  live: bool
}
