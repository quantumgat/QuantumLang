// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimePointerPinRecord {
  allocationId: u64
  generation: u64
  pinned: bool
  moveRejected: bool
}
