// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct BaremetalSyncRuntime {
  target: string
  criticalSection: bool
  interruptsMasked: bool
  blockingForbidden: bool
}
