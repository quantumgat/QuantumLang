// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeCpuFeatureSet {
  target: string
  detectedFeatures: usize
  staticFeatures: usize
  requiredFeatures: usize
}
