// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimePlatformRegistry {
  targetCount: usize
  defaultTarget: string
  rejectsUnknownTargets: bool
}
