// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeCachePlacement {
  address: usize
  cacheLineBytes: usize
  padded: bool
  falseSharingRisk: bool
}
