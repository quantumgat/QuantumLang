// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeTimeSource {
  target: string
  monotonicClock: bool
  wallClock: bool
  tickOnly: bool
}
