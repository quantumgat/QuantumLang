// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct QubitHandle {
  handleId: u64
  providerOwned: bool
  generationChecked: bool
}
pub extern "qnpkg" {
  pub fn qubitHandle(handleId: u64) -> QubitHandle
}
