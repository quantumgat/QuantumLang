// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeForeignDeleter {
  deleterId: u64
  allocatorDomain: string
  registered: bool
  lifetimeTokenRequired: bool
}
