// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeTlsSession {
  handle: u64
  resumed: bool
  verified: bool
  unsafeDisabledRejected: bool
  zeroCopyPlaintextEligible: bool
}
