// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct Deadline {
  unixMillis: u64
  active: bool
}
pub extern "qnpkg" {
  pub fn deadline(unixMillis: u64) -> Deadline
}
