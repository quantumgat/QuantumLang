// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct RuntimeTimerWheel {
  slotCount: usize
  tickNanos: u64
  activeTimers: usize
  cancellationLinked: bool
}
