// qtlc package API surface for runtime; bodies are supplied by the native package archive.
pub struct WorkShard {
  shardId: u64
  itemCount: usize
}
pub extern "qnpkg" {
  pub fn workShard(shardId: u64, itemCount: usize) -> WorkShard
}
